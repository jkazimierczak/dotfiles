packages:
  linux:
    apt:
      - git
      - git-lfs
      - tree
      - net-tools
      - htop
      - zsh
      - jq
      - unzip
      - wget
      # Required by Ansible
      - sshpass
      # Require adding APT sources
      - terraform
    snap:
      classic:
      strict:
