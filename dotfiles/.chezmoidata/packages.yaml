packages:
  linux:
    apt:
      - git
      - git-lfs
      - tree
      - net-tools # netstat
      - dnsutils # dig
      - htop
      - zsh
      - jq
      - unzip
      - wget
      - ncdu
      # Required by Ansible
      - sshpass
      # Requires adding APT sources
      - vault
      - terraform
    snap:
      classic:
      strict:
